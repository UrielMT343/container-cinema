apiVersion: apps/v1
kind: Deployment
metadata:
  name: frontend-proxy
spec:
  replicas: 2
  selector:
    matchLabels:
      app: frontend
  template:
    metadata:
      labels:
        app: frontend
    spec:
      containers:
        - name: nginx
          # The version is hardcoded as the source of truth
          image: urielmt343/cloudcinema-frontend:1.30.0-alpine
          ports:
            - containerPort: 8080
          envFrom:
            - configMapRef:
                name: app-config
